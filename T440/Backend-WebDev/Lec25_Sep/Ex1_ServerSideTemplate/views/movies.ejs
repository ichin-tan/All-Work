<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />    
</head>
<body>
    <h1>My movies!</h1>
    <% for(i=0; i<arrMovies.length; i++) { %>
        <p>Movie <%= i+1 %> is <%= arrMovies[i].title %></p>
        <p>Likes are <%= arrMovies[i].likes %></p>
        <!-- <p>The star rating is <%= arrMovies[i].starRating %></p> -->

        <% for(j = 0; j < arrMovies[i].starRating; j++) { %>
            <span class="material-symbols-outlined">
                star
            </span>
        <% } %>
        <br>
        <form action="/like" method="POST">
            <button type="submit">like</button>
        </form>    
        <form action="/star" method="POST">
            <input type="text" placeholder="Enter star rating" name="updatedStarRating">
            <button type="submit">Enter</button>
        </form>    
    <% } %>

</body>
</html>